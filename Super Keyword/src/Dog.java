public class Dog {
    // creating 2 new Variables in the Dog class
    String eat;
    double slim;

    //creating a constructor to hold all the parameters from the cat class(using super keyword)
    Dog(double slim,String eat) {
        //making a block of code which will assign the 2 variables with the parameters
        this.slim = slim;
        this.eat = eat;

    }
}
